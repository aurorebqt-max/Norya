import { useEffect, useState } from 'react';
import { View, Pressable } from 'react-native';
import { Screen, Card, Txt, Chip, Button, Icon, RowLink, Progress, Columns, s } from '../components/ui';
import { podcasts } from '../data/demo';
import { useDemo } from '../state/DemoProvider';
import { colors as c } from '../design/theme';
export default function Podcasts(){const [selected,setSelected]=useState(podcasts[0]);const [playing,setPlaying]=useState(false);const [elapsed,setElapsed]=useState(0);const [style,setStyle]=useState('Conversation');const [duration,setDuration]=useState('15 min');const [voice,setVoice]=useState('Camille');const [playlist,setPlaylist]=useState('Tous');const [configured,setConfigured]=useState(false);const {complete,done}=useDemo();useEffect(()=>{if(!playing)return;const timer=setInterval(()=>setElapsed(v=>{if(v>=selected.minutes*60){setPlaying(false);return v}return v+1}),1000);return()=>clearInterval(timer)},[playing,selected]);const time=(n:number)=>Math.floor(n/60)+':'+String(n%60).padStart(2,'0');return <Screen title="Vos révisions ont une voix" subtitle="Un moment pour écouter. Une autre façon de retenir." back><View style={s.wrap}>{['Tous','Les essentiels','Mes erreurs'].map(x=><Chip key={x} label={x} selected={playlist===x} onPress={()=>setPlaylist(x)}/>)}</View><Columns main={<><View style={{flexDirection:'row',flexWrap:'wrap',gap:16}}>{podcasts.filter(x=>playlist==='Tous'||(playlist==='Mes erreurs'?x.id==='p2':x.id!=='p2')).map(x=><Pressable key={x.id} style={{flexBasis:220,flexGrow:1}} onPress={()=>{setSelected(x);setElapsed(0);setPlaying(false)}}><Card style={{backgroundColor:x.color,borderColor:selected.id===x.id?c.primary:x.color,minHeight:235}}><View style={s.between}><Txt size={10} bold>NORYA AUDIO</Txt><Icon name="headset-outline"/></View><View style={{flexDirection:'row',height:55,alignItems:'center',justifyContent:'center',gap:5}}>{[12,25,37,22,50,32,46,20,38,25,13].map((h,i)=><View key={i} style={{width:5,height:h,borderRadius:5,backgroundColor:c.primary,opacity:.3+i%3*.2}}/>)}</View><Txt size={21} bold>{x.title}</Txt><Txt size={12}>{x.subtitle} · {x.minutes} min</Txt></Card></Pressable>)}</View><Card><Txt bold size={20}>À votre façon</Txt><Txt size={13} color={c.muted}>Configurez un futur podcast. La génération audio n’est pas activée.</Txt><Txt bold size={13}>Style</Txt><View style={s.wrap}>{['Conversation','Synthèse','Quiz oral'].map(x=><Chip key={x} label={x} selected={style===x} onPress={()=>{setStyle(x);setConfigured(false)}}/>)}</View><Txt bold size={13}>Durée</Txt><View style={s.wrap}>{['5 min','15 min','25 min'].map(x=><Chip key={x} label={x} selected={duration===x} onPress={()=>{setDuration(x);setConfigured(false)}}/>)}</View><Txt bold size={13}>Voix</Txt><View style={s.wrap}>{['Camille','Alex'].map(x=><Chip key={x} label={x} selected={voice===x} onPress={()=>{setVoice(x);setConfigured(false)}}/>)}</View><Button title="Prévisualiser ma configuration" secondary onPress={()=>setConfigured(true)}/>{configured&&<Txt color={c.primary}>Votre préférence : {style}, {duration}, voix {voice}. Aperçu uniquement, aucun audio généré.</Txt>}</Card></>} side={<Card><Txt size={12} color={c.muted}>LE LECTEUR · SIMULATION SANS SON</Txt><View style={{height:160,backgroundColor:selected.color,borderRadius:18,alignItems:'center',justifyContent:'center'}}><Icon name="headset-outline" size={68} color={c.primary}/></View><Txt bold size={23}>{selected.title}</Txt><Txt size={13} color={c.muted}>{selected.subtitle}</Txt><Progress value={elapsed/(selected.minutes*60)*100}/><View style={s.between}><Txt size={12}>{time(elapsed)}</Txt><Txt size={12}>{selected.minutes}:00</Txt></View><View style={[s.row,{justifyContent:'center'}]}><Pressable accessibilityLabel="Reculer de 15 secondes" onPress={()=>setElapsed(Math.max(0,elapsed-15))}><Icon name="play-back-outline"/></Pressable><Button title={playing?'Pause':'Lecture'} icon={playing?'pause':'play'} onPress={()=>setPlaying(!playing)}/><Pressable accessibilityLabel="Avancer de 15 secondes" onPress={()=>setElapsed(Math.min(selected.minutes*60,elapsed+15))}><Icon name="play-forward-outline"/></Pressable></View>{selected.id==='p2'&&<Button title={done.includes('audio')?'Séance audio terminée':'Marquer la révision comme terminée'} secondary onPress={()=>{complete('audio');setPlaying(false);setElapsed(selected.minutes*60)}}/>}<Txt size={11} color={c.muted}>Le lecteur illustre les commandes. Aucun fichier sonore ou service de synthèse vocale n’est connecté.</Txt></Card>}/></Screen>}
